# P8599 带分数

- **题目链接**：[洛谷 P8599](https://www.luogu.com.cn/problem/P8599)
- **知识点**：DFS / 全排列 / 回溯
- **输入输出**：
  - 输入：一个整数 N (N < 10^6)
  - 输出：带分数表示法的数量
- **题目分析**：
  1. 用 1~9 全排列组成数字序列
  2. 切分为 A | B | C
  3. 判断公式: `A + B / C == N`（为了避免浮点数用 `A*C + B == N*C`）
  4. 满足条件就计数
- **算法模板（C++）**：
  //DFS核心代码
```cpp
#include <bits/stdc++.h>
using namespace std;
//DFS
int a[10];//a表示最终的排列
bool used[10];//used标记1-9中某个数字是否被用了
void dfs(int step){//step表示当前要填第几个位置
if(step==9){//都到第九位了就把这一种排列进行输出
    for(int i=0;i<9;i++) cout<<a[i];
    cout<<endl;
    return ;
}
    for(int i=1;i<=9;i++){//第step位置尝试放入1到9中的任意一个
        if(!used[i]){
            used[i]=true;
            a[step]=i;
            dfs(strp+1);
            used[i]=false;//此位置填入的数进行撤销，填入下一位数
        }
    }
}
```
- 解题代码
  ```cpp
  int getNum(int l,int r){
    int x=0;
    for(int i=l;i<=r;i++){
        x=x*10+a[i];
        
    }
    return x;
  }
  void dfs(int step){
    if(step==9){//已经到最后一位数了
        //切A/B/C每一位至少有一位数，所以
        for(int i=0;i<7;i++){//A
            for(int j=i+1;j<8;j++){//B
                int A=getNum(0,i);
                int B=getNum(i+1,j);
                int C=getNum(j+1,8);
                if(A*C+B==N*C) ans++;

            }
        }
        return;
        
    }

    for(int i=1;i<=9;i++){
        if(!used[i]){
            used[i]=true;
            a[step]=i;
            dfs(step+1);
            used[i]=false;
        }
    }
  }
```
