# 二分问题
## while(r>=l)还是while(r-l>1e-7)?
- ![alt text](image.png)
- 当然这里只是举个例子，具体情况具体分析 
## 整数二分：在有序数组中查找元素，答案是整数
- 经典二分查找
- while(l<=r) while(l<r)
```cpp
int l=0,r=n-1;
while(l<=r){
    int mid=(l+r)/2;
    if(arr[mid]==target) return mid;
    else if(arr[mid]<target) l=mid+1;
    else r=mid-1;
}
return -1;
```
反正情况是很多的，灵神大佬有视频讲过
## 实数二分 while(r-l>eps) 求方程的根，求函数的极值点，以及任何需要实数且无法精确表示的问题，精度足够就停止
- 一元三次方程求解
```cpp
while(r-l>1e-7){
    double mid=(l+r)/2;
    if(f(mid)*f(r)<0) l=mmid;//根在[mid,r]
    else r=mid; //根在[l,mid]
    
}
```
```cpp
//不要混用该转换为double就要转为doule ,一定一定要注意格式
        double l=i,r=i+1;
        double f1=f(l);
        double f2=f(r);
%.02d输出两位整数，不足补0，常写为%02d
%。02f小数点后保留两位
```
# A-B问题
前提：有序序列，从小到大
lower_bound(begin,end,val) 找到第一个>=val的位置，返回第一个不小于val的元素的位置
upper_bound(begin,end,val) 找到第一个>val的位置，返回第一个大于val的元素的位置

## MOD的使用
-- 爆long long ，相乘数增加速度快，但是容易溢出，所以要用%取模，防止溢出
-- 给谁用MOD？会变大的变量都要进行取模

# 前缀和
```cpp
#include<bits/stdc++.h>
using namespace std;
int main(){
    int n,m;
    long long k;
    cin>>n>>m>>k;
    vector<vector<int>>a(n,vector<int>(m));
    for(int i=0;i<n;i++){
        for(int j=0;j<m;j++){
            cin>>a[i][j];
        }
    }

    long long ans=0;
    vector<long long >sum(m);

    for(int top=0;top<n;top++){
        fill(sum.begin(), sum.end(), 0); // 每个 top 初始化列和为0

        for(int bottom=top;bottom<n;bottom++){
            // 更新列和
            for(int j=0;j<m;j++){
                sum[j] += a[bottom][j];
            }

            // 双指针统计连续子数组和 <= k
            long long curr = 0;
            int left = 0;
            for(int right=0; right<m; right++){
                curr += sum[right];
                while(curr > k){
                    curr -= sum[left];
                    left++;
                }
                ans += right - left + 1; // 当前以 right 结尾的合法区间个数
            }
        }
    }

    cout<<ans<<endl;
    return 0;
}
```
# 状态DP
## 状态定义
f[i] 铺满前i列，且第i列是完整的方案数
g[i] 铺满前i列，且第i列的上方格子是空的，下方各自被覆盖的方案数

初始状态 f[0]=1 空画布，0列铺满，1种方案
        f[1]=1 只有一列，只能放一个竖i型
        g[0]=0 不可能
        g[1]=0
## 转移方程
### 如何得到f[i]
情况A：
前i列已经完整，从f[i-1]转移，
第i列放两个横i型，需要前i-2列完整，从f[i-2]转移
情况B：
前一列缺失一格g[i-1] ，一个L型刚好把上一列缺的一格和当前列的完整两个填满，得到当前列完整，所以从g[i-1]转移
因此
f[i]=f[i-1]+f[i-2]+g[i-1];
### 如何得到g[i] 第i列缺失一格
情况A：前一列完整，用L型放在前i-1列，让他凸出到第i列的上方一格，得到第i列缺失一格，这种L型有2种旋转方式，但是要使得第i列只缺一格，需要前i-2列完整，然后放一格L型跨两列，f[i-2]*2种
情况B：前一列已经缺失一格，用L型平着放，会填满前一列的缺口，同时在当前列产生一个缺口，这只有一种方式，从g[i-1]转移
g[i]=2*f[i-2]+g[i-1];
### emmm分析完后写代码很简单，但是这个分析过程是人脑想出来的？？？
```cpp
#include <bits/stdc++.h>
using namespace std;

const int MOD = 1e9 + 7;
Codeforces
int main() {
    int N;
    cin >> N;

    if (N == 1) {
        cout << 1 << endl;
        return 0;
    }

    long long f0 = 1;  // f[i-2]
    long long f1 = 1;  // f[i-1]
    long long g0 = 0;  // g[i-2]
    long long g1 = 0;  // g[i-1]

    for (int i = 2; i <= N; i++) {
        long long f2 = (f1 + f0 + g1) % MOD;
        long long g2 = (2 * f0 + g1) % MOD;

        // 滚动
        f0 = f1;
        f1 = f2;
        g0 = g1;
        g1 = g2;
    }

    cout << f1 % MOD << endl;

    return 0;
}
```
# 8786 李白打酒加强版 同样也是一道DP，但是是三维
## 状态定义
dp[i][j][k]=已经遇到i次店，j次花，当前酒量为k的方案数
## 初始状态 dp[0][0][2]=1 
## 答案 dp[n][m-1][1] 
## 转移方程推导 这里用+=是把所有的方案数累加起来
1.下一步遇到酒店，k*2<=m转移dp[i+1][j][k*2]+=dp[i][j][k];
2.下一步遇到花,dp[i][j+1][k-1]+=dp[i][j][k];
```cpp

#include <bits/stdc++.h>
using namespace std;

const int MOD = 1e9 + 7;

int main() {
    int N, M;
    cin >> N >> M;

    // dp[i][j][k]: i次店, j次花, 当前酒量k
    vector<vector<vector<int>>> dp(N+1, vector<vector<int>>(M+1, vector<int>(M+2, 0)));

    dp[0][0][2] = 1;

    for (int i = 0; i <= N; i++) {
        for (int j = 0; j <= M; j++) {
            for (int k = 0; k <= M; k++) {  // 酒量超过 M 无意义
                if (dp[i][j][k] == 0) continue;

                // 遇店
                //i==N说明已经遇到所有的店了不能再遇到店
                if (i < N && k * 2 <= M) {
                    dp[i+1][j][k*2] = (dp[i+1][j][k*2] + dp[i][j][k]) % MOD;
                }

                // 遇花（不能是最后一次花？不，这里正常转移，最后答案特殊取）
                if (j < M && k > 0) {
                    dp[i][j+1][k-1] = (dp[i][j+1][k-1] + dp[i][j][k]) % MOD;
                }
            }
        }
    }

    // 最后一次必须是花：前 N+M-1 次后酒量 = 1
    cout << dp[N][M-1][1] % MOD << endl;

    return 0;
}
```

# 13934 数组切分  区间合法性+DP
## 状态定义
 dp[i] = 前i个数的切分方案数
## 初始状态
dp[0]=dp[1]=1
## 状态转移方程 
如果[j....i]是合法的连续数
那么dp[i]+=dp[j-1]
就是这件事得慢慢想清楚，合法区间右边是i，左边是要进行枚举的，是的话就要加上去，这样就能不落下任何一个合法区间的方案数。
## 数学性质
排列性质：如果一段数是连续的，就满足max-min+1==长度
```cpp
#include <bits/stdc++.h>
using namespace std;

const long long MOD = 1e9 + 7;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    int n;
    cin >> n;

    vector<int> a(n + 1);
    for(int i = 1; i <= n; i++)
        cin >> a[i];

    vector<long long> dp(n + 1, 0);
    dp[0] = 1;

    for(int i = 1; i <= n; i++){
        int mx = a[i], mn = a[i];

        for(int j = i; j >= 1; j--){
            mx = max(mx, a[j]);
            mn = min(mn, a[j]);

            int len = i - j + 1;

            if(mx - mn + 1 == len){
                dp[i] = (dp[i] + dp[j-1]) % MOD;
            }
        }
    }

    cout << dp[n] << endl;
}
```