# 10387训练士兵

- **题目链接**：[洛谷 P8599](https://www.luogu.com.cn/problem/P10387)
- **知识点**：贪心/枚举组团次数

- **题目分析**：

- **算法模板（C++）**：
1.暴力枚举,枚举不同团体训练次数下的花费，部分测试点通过，部分测试点显示超时
```cpp
#include <bits/stdc++.h>
using namespace std;
int main(){
//提高输入输出速度的优化代码
ios::sync_with_stdio(false);
cin.tie(NULL);
int n;
long long S;
cin >> n >> S;
vector<long long> p(n),c(n);
long long maxc=0;
for(int i=0;i<n;i++){
    cin>>p[i]>>c[i];
    maxc=max(maxc,c[i]);

}
//需要进行的最多次数的训练数maxc
long long ans=LLONG_MAX;
for(long long k=0;k<=maxc;k++){
    long long cost=k*S;//进行团体训练
    for(int i=0;i<n;i++){
        if(c[i]>k){
            cost+=(c[i]-k)*p[i];//单独训练士兵费用
        }
    }
    ans=min(ans,cost);
}
    cout<<ans<<endl;
    return 0;
}
```
2.
```cpp
#include <bits/stdc++.h>
using namespace std;
ytpedef long long ll;
int main(){
   ios::sync_with_stdio(false);
   cin.tie(NULL);
   int n;
   
   ll S;
   cin>>n>>S;
   int max_c=0;
   vector<pair<int,int>>soldiers(n);
   for(int i=0;i<n;i++){
    cin>>soldiers[i].second>>soldiers[i].first;
    //次数是first,单次花费是second
    max_c=max(max_c,soldiers[i].first);//max_c需要经过的最多次的训练次数
    }
    sort(soldiers.begin(),soldiers.end(),greater<pair<int,int>>());//greater<pair<int,int>>从大到小进行排序，先比较first, 再比较second
    vector<ll> sum_cp(n+1,0);
    vector<ll> sum_p(n+1,0);
    for(int i=0;i<n;i++){
        //sum_cp就是每个士兵单独训练后需要的钱
        sum_cp[i+1]=sum_cp[i]+soldiers[i].second*soldiers[i].first;
        //所有士兵需要的训练次数和
        sum_p[i+1]=sum_p[i]+soldiers[i].second;
    }
    ll ans=LLONG_MAX;
    for(int k=0;k<=max_c;k++){
        int l=0,r=n;
        //二分法找到c<=k
        while(l<r){
            int mid=(l+r)/2;
            if(soldiers[mid].first<=k){
                r=mid;
            }else{
                l=mid+1;
            }
        }
        ll cost=(ll)k*S;
        if(l>0){
            cost+=sum_cp[l]-(ll)k*sum_p[l];
        }
        ans=min(ans,cost);
    }
    cout<<ans<<endl;
    return 0;
    }
```
**自定义比较函数**
- Lambda表达式
```cpp
  sort(soldiers.begin(),soldiers.end(),greater<pair<int,int>>());
  sort(soliders.begin(),soliders.end(),[](const pair<int,int>& a,const pair<int,int>&b){
    if(a.first!=b.first){
        return a.first>b.first;
    }
    return a.second>b.second;
});
```
# 10902 回文数组
- **题目链接**：[洛谷 P10902](https://www.luogu.com.cn/problem/P10902)
```cpp
#include <bits/stdc++.h>
using namespace std;
// const int N=1e5+10;
// long long n,a[N],b[N],ans;
int main(){
    long long n=0,ans=0;
    cin>>n;
    vector<long long> a(n+2),b(n+2);
    for(int i=1;i<=n;i++){
        cin>>a[i];

    }
    for(int i=1;i<=(n+1)/2;i++){//就遍历一半
        if(a[i]<a[n-i+1]) b[i]=a[n-i+1]-a[i];//b[]用来存储差值,i处比另一个地方少多少
        else b[n-i+1]=a[i]-a[n-i+1];

    }
    for(int i=1;i<=n;i++){
        int mn=min(b[i],b[i+1]);
        //mn为相邻两个数可以同时加1的次数
        b[i]-=mn;//加完1就可以增大了
        b[i+1]-=mn;
        ans+=mn;
        ans+=b[i];
    }
    cout<<ans;
    return 0;
}



