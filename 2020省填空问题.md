**门牌号**
- 整数->字符串
- 方法一：to_string()
 ```cpp
int x=12345;
string s=to_string(x);
```
方法二：手动转(进制题)
```cpp
int x=123;
string s;
while(x){
    s=char(x%10+'0')+s;
    x/=10;
}
- 字符串->整数
- 方法一：stoi()
```cpp
string s="12345";
int x=stoi(s);
```
方法二： 手动转
```cpp
string s="12345";
int x=0;
for(char c:s){
    x=x*10+c-'0';
}
- **判断一个数中是否包含某个数字**
方法一：转字符串进行判断
```cpp
int x=12345;
string s=to_string(x);
//npos not position of string
if(s.find("6")!=string::npos){
    cout<<"Yes"<<endl;
}else{
    cout<<"No"<<endl;
}
```
方法二：位运算
```cpp
int x=12345;
if(x&1<<3){
    cout<<"Yes"<<endl;
}else{
    cout<<"No"<<endl;
}
```
***最大公约数**
- 方法一：辗转相除法(欧几里得算法)
```cpp
int gcd(int a,int b){
    return b==0?a:gcd(b,a%b);
}
cout<<gcd(12,8)<<endl; //输出4
```
- 方法二：c++自带函数
  ```cpp
  #include<algorithm>
  cout<<__gcd(12,18);
  //最小公倍数
  int l=a/gcd(a,b)*b;
  ```





A:624
B:2481215
C:2965    761
D:8879