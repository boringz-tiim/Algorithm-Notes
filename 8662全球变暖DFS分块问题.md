# 洛谷8662 全球变暖

- **题目链接**：https://www.luogu.com.cn/problem/P8662
- **知识点**：DFS 连通块（逐个岛屿考虑是否被淹没）

- **题目分析**：
  1.遍历整个地图，遇到没访问过的#，用DFS遍历岛屿，判断是否都会被淹没如果会则+1.
  

- **算法模板（C++）**：
```cpp
#include <bits/stdc++.h>
using namespace std;
vector<string> g;
vector<vector<int>> vis;
int dx[4]={1,-1,0,0};
int dy[4]={0,0,1,-1};
void dfs(int x,int y){
    vis[x][y]=1;
    for(int i=0;i<4;i++){
        int nx=x+dx[i];
        int ny=y+dy[i];
        //没有访问过的#就进行访问
        if(nx>=0&&nx<n&&ny>=0&&ny<m&&!vis[nx][ny]&&g[nx][ny]=='#'){
            dfs(nx,ny);
        }
    }
}

// ...核心代码...
int ans=0;

for(int i=0;i<n;i++)
{
    for(int j=0;j<m;j++)
    {
        if(g[i][j]=='#' && !vis[i][j])
        {
            dfs(i,j);
            ans++;
        }
    }
}
```
# 洛谷1451求细胞数量

- **题目链接**：https://www.luogu.com.cn/problem/P1451#ide
```cpp
#include<bits/stdc++.h>
using namespace std;
int n,m;
vector<vector<int>>myvec(n,vector<int>(m));

vector<vector<int>>visit;
int nx[4]={-1,1,0,0};
int ny[4]={0,0,-1,1};
void dfs(int i,int j){
    visit[i][j]=1;
    for(int z=0;z<4;z++){
        int x=i+nx[z];
        int y=j+ny[z];
        if(x>=0&&x<n&&y>=0&&y<m&&myvec[x][y]!=0&&!visit[x][y]){
            dfs(x,y);
        }
    }
}
int main(){
    cin>>n>>m;
       myvec = vector<vector<int>>(n, vector<int>(m));
    visit = vector<vector<int>>(n, vector<int>(m, 0));  // 初始化为0
    //多想留在你的身边
       // 修正：按字符串读入
    for(int i = 0; i < n; i++) {
        string s;
        cin >> s;
        for(int j = 0; j < m; j++) {
            myvec[i][j] = s[j] - '0';  // 字符转数字
        }
    }
    
   // visit=vector<vector<int>>(n,vector<int>(m));
    int ans=0;
    for(int i=0;i<n;i++){
        for(int j=0;j<m;j++){
            if(myvec[i][j]!=0&&!visit[i][j]){
                dfs(i,j);
                ans++;
            }
        }
    }
    cout<<ans;
    return 0;
    
}
```
# 洛谷1596水塘数目 

- **题目链接**：https://www.luogu.com.cn/problem/P1596
- **题目分析**：八个方向DFS
 ```cpp
#include<bits/stdc++.h>
using namespace std;
int n,m;
int dx[8]={1,1,1,0,0,-1,-1,-1};
int dy[8]={1,-1,0,1,-1,0,-1,1};
vector<string>myvec(n);
vector<vector<int>>vis;
void dfs(int x,int y){
    vis[x][y]=1;
    for(int i=0;i<8;i++){
        int nx=x+dx[i];
        int ny=y+dy[i];
         if(nx>=0&&nx<n&&ny>=0&&ny<m
        &&!vis[nx][ny]&&myvec[nx][ny]=='W')
        {
            dfs(nx,ny);
        }
    }
}

int main(){
    cin>>n>>m;
    myvec=vector<string>(n);
    vis=vector<vector<int>>(n,vector<int>(m,0));
    for(int i=0;i<n;i++){
        cin>>myvec[i];
    }
    int ans=0;
    for(int i=0;i<n;i++){
        for(int j=0;j<m;j++){
            if(myvec[i][j]=='W'&&!vis[i][j]){
                 ans++;
                dfs(i,j);
               // ans++;
            }
        }
    }
            cout<<ans;
            return 0;
}
```

# 洛谷1162填涂颜色

- **题目链接**：https://www.luogu.com.cn/problem/P1162
- **题目分析**：DFS算法/边界条件处理/连通性问题/二维矩阵
- **算法模板（C++）**：
- 1.从一个从未遍历过的0出发，想上下左右四个方向走
- 2.只能走0，如果遇上边界上的0，说明能出去，不封闭，isClosed=false;
- 3.如果走完所有的路都没有遇到边界，就是封闭的 return isClosed=true;
- **算法模板**
```cpp
bool dfs(int i,int j){
    
    vis[i][j]=1;
    bool isclosed=true;
     if(i == 0 || i == n-1 || j == 0 || j == n-1) {
        isclosed=false;
    }
    
    
    for(int z=0;z<4;z++){
        int x=i+nx[z];
        int y=j+ny[z];
        if(x < 0 || x >= n || y < 0 || y >= n)continue;
        if(!vis[x][y]&&myvec[x][y]=='0'){
            
if(!dfs(x,y)){
    isclosed=false;}
}

        
    }
    return isclosed;
}
```
# 洛谷1506拯救oibh总部

- **题目链接**：https://www.luogu.com.cn/problem/P1506
- **题目分析**：DFS算法/连通性问题/二维矩阵
- **代码**
- **注意** 不要只关注相邻点是不是边界，要看当前连通块是否在边界，也就是当前DFS节点在边界，而不是邻居在边界
```cpp
bool dfs(int i,int j){
    visit[i][j]=1;
    bool isClosed=true;

    // 当前点在边界 -> 不封闭
    if(i==0 || i==x-1 || j==0 || j==y-1){
        isClosed=false;
        //为什么不直接返回false?还要继续往下遍历
        //因为DFS不只是判断是否封闭，还承担了标记整个连通块已经访问的任务
    }

    for(int z=0;z<4;z++){
        int ix=i+nx[z];
        int iy=j+ny[z];

        if(ix<0||ix>=x||iy<0||iy>=y){
            continue;
        }

        if(myvec[ix][iy]=='0'&&!visit[ix][iy]){
            if(!dfs(ix,iy)){
                isClosed=false;
            }
        }
    }
    return isClosed;
}
```
//把统计不被淹没陆地一起放进函数中
```cpp
int dfs(int i,int j,bool &closed){
    visit[i][j]=1;
    int cnt=1;

    if(i==0||i==x-1||j==0||j==y-1){
        closed=false;
    }

    for(int k=0;k<4;k++){
        int nx1=i+nx[k];
        int ny1=j+ny[k];

        if(nx1>=0 && nx1<x && ny1>=0 && ny1<y &&
           myvec[nx1][ny1]=='0' && !visit[nx1][ny1]){
            cnt+=dfs(nx1,ny1,closed);
        }
    }
    return cnt;
}
int ans=0;
for(int i=0;i<x;i++){
    for(int j=0;j<y;j++){
        if(!visit[i][j] && myvec[i][j]=='0'){
            bool closed=true;
            int cnt=dfs(i,j,closed);
            if(closed) ans+=cnt;
        }
    }
}
```

```cpp
class Solution {
public:
    int numIslands(vector<vector<char>>& grid) {
        int m=grid.size(),n=gird[0].size();
        auto dfs=[&](this auto&& dfs,int i,int j)->void{
            if(i<0||i>=m||j<0||j>=n||grid[i][j]!='1'){
                return ;
            }
            grid[i][j]='2';
            dfs(i,j-1);
            dfs(i,j+1);
            dfs(i-1,j);
            dfs(i+1,j);
        };
        int ans=0;
        for(int i=0;i<m;i++){
            for(int j=0;j<n;j++){
                dfs(i,j);
                ans++;
            }
        }
        return ans
        
        
        
        
        
        ;
    }
};
```

🔵 进阶：洪水扩散 / 反向 DFS

这些题思路更优雅（推荐重点练）

P1219 八皇后
虽然不是网格，但练 DFS 思维
P2895 Meteor Shower S
BFS + 扩散（DFS 思维升级）
🟣 同类 LeetCode（如果你以后刷）

这些就是同一模型：

封闭岛屿（Closed Island）
岛屿数量（Number of Islands）
被围绕的区域（Surrounded Regions）
飞地的数量（Number of Enclaves）

-- DFS的常见分类
网格DFS、排列型DFS、组合型DFS、树型DFS

# 9241飞机降落 排列型DFS
- **知识点**：排列型DFS / 全排列 / 回溯

- **题目分析**：
 
- **算法模板（C++）**：
```cpp
#include <bits/stdc++.h>
using namespace std;

struct Plane {
    int T, D, L;
};

int n;
vector<Plane> planes;
bool vis[15];//用来标记哪一架飞机已经降落过了
bool ok;

void dfs(int u, int time) {
    if (ok) return;
    if (u == n) {
        ok = true;
        return;
    }

    for (int i = 0; i < n; i++) {
        if (vis[i]) continue;

        int start = max(time, planes[i].T);
        if (start > planes[i].T + planes[i].D) continue; // 来不及

        vis[i] = true;
        dfs(u + 1, start + planes[i].L);
        vis[i] = false;
    }
}

int main() {
    int T;
    cin >> T;
    while (T--) {
        cin >> n;
        planes.resize(n);
        for (int i = 0; i < n; i++) {
            cin >> planes[i].T >> planes[i].D >> planes[i].L;
        }

        memset(vis, 0, sizeof(vis));//重置所有标记为false
        ok = false;
        dfs(0, 0);

        cout << (ok ? "YES" : "NO") << endl;
    }
    return 0;
}
```

```cpp
void *memset(void *s, int c, size_t n);
//s 要填充的内存的起始地址
//c 要填充的字符
//n 要填充的字符的个数
//等价写法
// 方法1：memset
memset(vis, 0, sizeof(vis));

// 方法2：for 循环
for (int i = 0; i < 15; i++) vis[i] = false;

// 方法3：fill 函数
fill(vis, vis + 15, false);
```