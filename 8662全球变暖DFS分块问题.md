# 洛谷8662 全球变暖

- **题目链接**：https://www.luogu.com.cn/problem/P8662
- **知识点**：DFS 连通块（逐个岛屿考虑是否被淹没）

- **题目分析**：
  1.遍历整个地图，遇到没访问过的#，用DFS遍历岛屿，判断是否都会被淹没如果会则+1.
  

- **算法模板（C++）**：
```cpp
#include <bits/stdc++.h>
using namespace std;
vector<string> g;
vector<vector<int>> vis;
int dx[4]={1,-1,0,0};
int dy[4]={0,0,1,-1};
void dfs(int x,int y){
    vis[x][y]=1;
    for(int i=0;i<4;i++){
        int nx=x+dx[i];
        int ny=y+dy[i];
        //没有访问过的#就进行访问
        if(nx>=0&&nx<n&&ny>=0&&ny<m&&!vis[nx][ny]&&g[nx][ny]=='#'){
            dfs(nx,ny);
        }
    }
}

// ...核心代码...
int ans=0;

for(int i=0;i<n;i++)
{
    for(int j=0;j<m;j++)
    {
        if(g[i][j]=='#' && !vis[i][j])
        {
            dfs(i,j);
            ans++;
        }
    }
}
```
# 洛谷1451求细胞数量

- **题目链接**：https://www.luogu.com.cn/problem/P1451#ide
```cpp
#include<bits/stdc++.h>
using namespace std;
int n,m;
vector<vector<int>>myvec(n,vector<int>(m));

vector<vector<int>>visit;
int nx[4]={-1,1,0,0};
int ny[4]={0,0,-1,1};
void dfs(int i,int j){
    visit[i][j]=1;
    for(int z=0;z<4;z++){
        int x=i+nx[z];
        int y=j+ny[z];
        if(x>=0&&x<n&&y>=0&&y<m&&myvec[x][y]!=0&&!visit[x][y]){
            dfs(x,y);
        }
    }
}
int main(){
    cin>>n>>m;
       myvec = vector<vector<int>>(n, vector<int>(m));
    visit = vector<vector<int>>(n, vector<int>(m, 0));  // 初始化为0
    //多想留在你的身边
       // 修正：按字符串读入
    for(int i = 0; i < n; i++) {
        string s;
        cin >> s;
        for(int j = 0; j < m; j++) {
            myvec[i][j] = s[j] - '0';  // 字符转数字
        }
    }
    
   // visit=vector<vector<int>>(n,vector<int>(m));
    int ans=0;
    for(int i=0;i<n;i++){
        for(int j=0;j<m;j++){
            if(myvec[i][j]!=0&&!visit[i][j]){
                dfs(i,j);
                ans++;
            }
        }
    }
    cout<<ans;
    return 0;
    
}
```