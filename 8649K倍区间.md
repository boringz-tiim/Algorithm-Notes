# P8649 K倍区间

- **题目链接**：[洛谷 P8649]https://www.luogu.com.cn/problem/P8649#ide
- **知识点**：前缀和/哈希计数

- **题目分析**：
-   1. **Brute force**: Enumerate all subarrays → O(n²) → too slow
  2. **Prefix Sum**:
     - Define `prefix[i] = A[1] + ... + A[i]`
     - Subarray sum: `sum[i..j] = prefix[j] - prefix[i-1]`
  3. **Modulo trick**:
     - `(prefix[j] - prefix[i-1]) % K == 0 ⇔ prefix[j] % K == prefix[i-1] % K`
     - Count pairs of equal remainders to get the number of valid subarrays
  4. **Counting**:
     - Use an array `cnt[mod]` to store how many times each remainder appears
     - For each prefix sum modulo K, `ans += cnt[mod]; cnt[mod]++;`
     - Initialize `cnt[0] = 1` to count subarrays starting at index 1

- **算法模板（C++）**：
```cpp
 vector<long long>cnt(K,0);//K个数，初始化为0
//就是还要注意本身模完就是零的数，要不就直接定义cnt[0]=1,要不到最后ans+=cnt[0]
for(int i=0;i<N;i++){
        int x;
        cin>>x;//输入一个算一个，不用提前建数组把数都放进去
        sum+=x;
        int mod=sum%K;
        ans+=cnt[mod];
        cnt[mod]++;
    }
```